*.iml
.gradle/
/local.properties
/.idea
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
local.properties

# Signing secrets - never commit these
keystore.properties
*.keystore
*.jks
*.apk

# Supabase project URL + anon key (read into BuildConfig)
supabase.properties

# Firebase project config for notifications (read into BuildConfig)
google-services.json

# Supabase CLI local cache
supabase/.temp/
